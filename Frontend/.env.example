# Backend API base URL (no trailing slash).
# Leave unset for local dev — Vite's proxy (vite.config.js) forwards /api to
# http://localhost:5000. Set explicitly for any deployed build.
VITE_API_URL=http://localhost:5000
